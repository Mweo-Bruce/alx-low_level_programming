#include "main.h"

/**
 * strlen - checks the length of a string
 * @s: string to store characters
 * @len: int to store value
 * Return: int
 */
int _strlen(char *s)
{
	int len = 0;

	while (*s != '\0')
	{
		long++;
		s++;
	}

	return(len);
}
